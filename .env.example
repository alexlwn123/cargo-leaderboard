# Vercel provisions this through its Neon integration.
DATABASE_URL=
# Generate a random value; required for production and preview deployments.
RATE_LIMIT_SALT=
# Optional: restrict submissions on a private installation. Public board leaves this unset.
CARGO_LEADERBOARD_TOKEN=
